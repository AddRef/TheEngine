Fix upack of boost. There are two options:
1. Build boost libraries for all the platforms and place them in separate archives
2. Create procedure that builds boost automatically for current platform from sources extracted from archive
